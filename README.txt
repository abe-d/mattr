Description

This function calculates the moving-window type-token ratio for a vector of sentences.

Usage

mattr(infile = ..., windowsize = ...)

Arguments

infile	the name of the file to read. Defaults to choosing from a dialogue box (on Windows).
windowsize	the size of the moving window to calculate the type-token ratios before averaging them. Defaults to 100.


